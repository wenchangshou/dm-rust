apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dm-rust-data-pvc
  labels:
    app: dm-rust
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # 如果使用特定的 StorageClass，取消下面的注释
  # storageClassName: standard
